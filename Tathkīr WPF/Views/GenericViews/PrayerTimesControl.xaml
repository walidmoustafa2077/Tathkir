<UserControl x:Class="Tathkīr_WPF.Views.GenericViews.PrayerTimesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Tathkīr_WPF"
             mc:Ignorable="d">
    <!-- Prayer list -->
    <Border CornerRadius="16"
            Background="#20F2FBFF"
            Padding="6">

        <StackPanel>
            <ItemsControl ItemsSource="{Binding PrayerList}"
                          Foreground="#103E65">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12"
                                Margin="0 2">

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsNextPrayer}"
                                                     Value="True">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource MaterialDesignDarkSeparatorBackground}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <materialDesign:PackIcon Kind="{Binding Icon}"
                                                         Width="24"
                                                         Height="24"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0" />

                                <!-- Prayer Name -->
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"
                                           Grid.Column="1" />

                                <!-- Time -->
                                <TextBlock Text="{Binding Time}"
                                           FontSize="16"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"
                                           Grid.Column="2" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Midnight and Last Third row -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Opacity="0.8"
                        Height="50">


                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock Foreground="#103E65"
                               Text="{x:Static local:Strings.Midnight}"
                               FontSize="14"
                               FontWeight="Medium"
                               Margin="0,0,4,0" />

                    <TextBlock Foreground="#103E65"
                               Text="{Binding MidnightTime}"
                               FontSize="14"
                               FontWeight="Bold" />

                    <TextBlock Foreground="#103E65"
                               Text="|"
                               FontSize="14"
                               Margin="18 0" />

                    <TextBlock Foreground="#103E65"
                               Text="{x:Static local:Strings.Last_Third}"
                               FontSize="14"
                               FontWeight="Medium"
                               Margin="0,0,4,0" />

                    <TextBlock Foreground="#103E65"
                               Text="{Binding LastThirdTime}"
                               FontSize="14"
                               FontWeight="Bold" />
                </StackPanel>

                <Button Command="{Binding ResetDateCommand}"
                        Foreground="Red"
                        Height="25"
                        Width="25"
                        Margin="8,0,0,0"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Visibility="{Binding ResetDateVisibility, Converter={StaticResource BoolToVisibilityConverter}}">
                    <materialDesign:PackIcon Kind="Restart"
                                             Width="20"
                                             Height="20" />
                </Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,6,0,0"
                        FlowDirection="LeftToRight">

                <!-- Left Arrow -->
                <Button Foreground="#103E65"
                        Command="{Binding PreviousDateCommand}"
                        Padding="4"
                        Margin="0,0,8,0"
                        Style="{StaticResource MaterialDesignIconForegroundButton}">
                    <materialDesign:PackIcon Kind="ChevronLeft"
                                             Width="30"
                                             Height="30" />
                </Button>

                <!-- Date -->
                <TextBlock Foreground="#103E65"
                           Text="{Binding CurrentDate}"
                           Width="300"
                           TextAlignment="Center"
                           FontSize="16"
                           FontWeight="Medium"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

                <!-- Right Arrow -->
                <Button Foreground="#103E65"
                        Command="{Binding NextDateCommand}"
                        Padding="4"
                        Margin="8,0,0,0"
                        Style="{StaticResource MaterialDesignIconForegroundButton}">
                    <materialDesign:PackIcon Kind="ChevronRight"
                                             Width="30"
                                             Height="30" />
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
